package com.softaria.quiz.service;

import com.softaria.quiz.model.LaunchRequest;
import org.springframework.stereotype.Service;
import org.springframework.transaction.annotation.Transactional;

@Service
@Transactional
public class LaunchRequestServiceImpl implements LaunchRequestService {

    public void saveLaunchRequest(LaunchRequest launchRequest) {

    }

}
